ext {
    android = [
            compileSdk   : 33,
            minSdk       : 23,
            targetSdk    : 33,
            versionCode  : 1,//这里是各个的默认配置，不是真正的版本号
            versionName  : "1.0",//这里是各个的默认配置，不是真正的版本号
            applicationId: "com.d08a3hqr.chtjikfd",
    ]
}